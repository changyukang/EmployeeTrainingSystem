<% provide(:name, 'All groups') %>
<h1>All groups</h1>


<ul class="users">
<%= will_paginate @groups %>  
  <% @groups.each do |group| %>
    <%= render group %>  
  <% end %>  
<%= will_paginate @group %>
</ul>

<% if current_user.manager? %>
  <%= link_to 'Create Group', new_group_path %> | 
  <%= link_to 'Assign User', new_user_group_path %>
<% end %>
