<% provide(:title, 'Courses') %>
<html>
    <head>
        <title>Courses</title>
    </head>
    <body>
        <h1>
            <% @user_groups.each do |user_group| %>
                <%= user_group.group_id%>  
            <% end %>  

            <% @array_courses.each do |array_course| %>
                <%= render array_course%>  
            <% end %> 
        </h1>
        <h4>
        </h4>
     
        <h4>
            <br><dt><%= "Wikis:" %></dt><br><br>

            <br><dt><%= "Quizzes:" %></dt><br><br>
        </h4>
        <h4>
        </h4>
    </body>
</html>
