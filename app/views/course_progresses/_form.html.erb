<%= form_with(model: course_progress, local: true) do |form| %>
  <% if course_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_progress.errors.count, "error") %> prohibited this course_progress from being saved:</h2>

      <ul>
        <% course_progress.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.number_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :progress %>
    <%= form.text_field :progress %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
